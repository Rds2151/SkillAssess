
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" /> <!-- Set the background color, e.g., transparent -->
    <stroke
        android:width="2dp"
        android:color="?attr/theme_color"
    />
</shape>
